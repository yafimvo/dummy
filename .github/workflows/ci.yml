name: CI

on: [push, pull_request]

jobs:
  test:
    strategy:
      matrix:
        python-version: ['3.10']
        os: [ubuntu-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .
          pip install ".[dev]"
      - name: Test with pytest
        run: |
          pytest

  # run: pkgmt check
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install 'pkgmt[check]'
      - name: Check project
        run: |
          pkgmt check

  release:
    runs-on: ubuntu-latest
    if: github.ref_name == 'release_test' && startsWith(github.event.head_commit.message, 'tag and release this version')
    needs: [test, check]
    steps:
      - name: Tag and Publish to PyPI if necessary
        env:
          TWINE_USERNAME: "__token__"
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          pip install wheel
          
          pip install twine

          git config user.email "github-actions@github.com"
          git config user.name "github-actions"

          echo "Bumping up tag version"
          pkgmt version --yes

          TAG=$(git describe --abbrev=0)
          echo "Releasing new version: $TAG"
          echo yes | pkgmt release $TAG --production
